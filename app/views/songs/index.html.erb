<h1>All The Songs</h1>

<p>This is the Song index page.</p>


<table>
  <tbody>
    <% @songs.each do |song| %>
    <tr><%= song.title %></tr>
    <%= song.released %>
    <%= song.release_year %>
    <%= song.artist_name %>
    <%= song.genre %>
    <% end %>
  </tbody>
</table>
